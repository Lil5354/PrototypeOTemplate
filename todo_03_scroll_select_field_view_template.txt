===================================================================
TODO 03 - Scroll không xuống hết khung Select Field khi bấm "+" ở View OKR Template
===================================================================

MÔ TẢ:
  - Hiện tại: Khi bấm dấu "+" ở giao diện View OKR Template,
    component dropdown/panel chọn field hiển thị bị che khuất,
    không thể scroll xuống hết để xem và chọn toàn bộ field.
  - Mong muốn: Có thể scroll xuống hết toàn bộ danh sách field
    trong khung select để xem và check đầy đủ.

HƯỚNG XỬ LÝ (STEP BY STEP):
  Bước 1: Xác định component dropdown/panel hiện ra khi bấm "+"
           ở View OKR Template.
  Bước 2: Kiểm tra CSS của container đó: overflow, max-height,
           position (absolute/fixed/relative), z-index.
  Bước 3: Nguyên nhân thường gặp:
           - Container cha có overflow: hidden cắt mất nội dung.
           - Panel không có max-height + overflow-y: auto/scroll.
           - Panel bị che bởi các element khác (z-index thấp).
  Bước 4: Thêm max-height phù hợp (vd: max-height: 300px hoặc
           tính toán theo viewport) và overflow-y: auto cho panel.
  Bước 5: Nếu bị cắt bởi container cha, điều chỉnh overflow
           của container cha hoặc dùng position: fixed cho panel.
  Bước 6: Test scroll tới cuối danh sách field.

RECHECK:
  [ ] Mở "+" ở View OKR Template → panel hiện ra đầy đủ
  [ ] Scroll xuống hết danh sách field không bị cắt
  [ ] Có thể check/uncheck field ở cuối danh sách
===================================================================
