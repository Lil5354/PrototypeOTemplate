===================================================================
TODO 07 - Full Screen OKR Tree: Popup Import không hiện ra được
===================================================================

MÔ TẢ:
  - Hiện tại: Ở giao diện Full Screen của OKR Tree, khi bấm Import,
    popup/modal Import không hiện ra được (bị ẩn phía sau overlay
    hoặc bị clip bởi container full screen).
  - Mong muốn: Import hoạt động bình thường trong chế độ Full Screen,
    tương tự như ở các giao diện khác không full screen.

HƯỚNG XỬ LÝ (STEP BY STEP):
  Bước 1: Xác định modal/popup Import đang được render ở đâu trong DOM.
  Bước 2: Nguyên nhân thường: Modal render bên trong container
           Full Screen có transform/filter/overflow:hidden → tạo ra
           stacking context mới, khiến z-index của modal bị giới hạn
           trong container đó.
  Bước 3: Giải pháp: Dùng React Portal để render modal Import
           trực tiếp vào document.body (thoát khỏi stacking context).
           Ví dụ: ReactDOM.createPortal(<ImportModal />, document.body)
  Bước 4: Đảm bảo z-index của modal đủ cao (vd: z-index: 9999).
  Bước 5: Nếu dùng thư viện UI (Ant Design, MUI...) → kiểm tra prop
           getContainer hoặc container để chỉ định render vào body.
  Bước 6: Test mở Import ở chế độ Full Screen, đảm bảo modal hiện đúng.

RECHECK:
  [ ] Bấm Import ở Full Screen OKR Tree → popup hiện ra đúng
  [ ] Popup không bị che khuất bởi container full screen
  [ ] Chức năng Import hoạt động đầy đủ trong chế độ Full Screen
  [ ] So sánh hành vi với giao diện không Full Screen → tương tự
===================================================================
